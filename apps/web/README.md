# Customer Web App

React + Vite starter for the customer web app.

## Run
1. Install dependencies in `apps/web`.
2. Copy `.env.example` to `.env` and set `VITE_API_URL`.
3. `npm run dev`.

## Notes
- Replace landing UI with actual product catalog, plan builder, and billing flows.
- Connect to API once backend is running.
