# Admin Dashboard

React + Vite starter for admin/staff dashboard.

## Run
1. Install dependencies in `apps/admin`.
2. Copy `.env.example` to `.env` and set `VITE_API_URL`.
3. `npm run dev`.

## Notes
- Replace sample dashboard with real data from API.
- Add delivery status updates, billing, and reporting workflows.
